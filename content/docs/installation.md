# Installation

Install the app or CLI locally. The snippets below are placeholders—swap in the exact commands for your release.

## Install via npm

```bash
npm install --global nela
nela --help
```

## Install a model (example)

```bash
nela models list
nela models download --name Small
```

