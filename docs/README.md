# Frontend docs

The marketing/account Next.js app lives here.

- Set `NEXT_PUBLIC_API_URL` to the NELA Cloud API (local default `http://localhost:3001`).
- API DTOs for the UI are defined in `lib/api-types.ts` — this repo does not share code with the backend.
